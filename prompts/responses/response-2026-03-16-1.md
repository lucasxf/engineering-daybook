# Response — 2026-03-16 (session start)

Both are unfixable on your side — they're Claude Code CLI bugs. I'd recommend filing one GitHub issue covering both at https://github.com/anthropics/claude-code/issues with the evidence I found:

- **PATH bug**: Claude Code's bash subprocess is non-login/non-interactive, inheriting a truncated process PATH (~7 entries) that excludes entries properly set in both Machine and User PATH registries. `/doctor` then warns about its own broken inheritance.
- **Stale autocomplete**: No `review.md` file exists anywhere, but `/review` still appears in autocomplete suggestions — likely a stale client-side index.

Both are cosmetic — claude.exe runs fine and all your commands work correctly.
