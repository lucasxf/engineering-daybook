/**
 * Minimal expo-constants mock for the 'screens' jest project (node env).
 */
const Constants = {
  expoConfig: {
    extra: {
      apiUrl: 'http://localhost:8080/api/v1',
    },
  },
};

module.exports = Constants;
module.exports.default = Constants;
