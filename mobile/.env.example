# Copy to .env.local and fill in values
# All EXPO_PUBLIC_* vars are bundled into the app — do NOT store secrets here

# Backend API base URL (no trailing slash)
EXPO_PUBLIC_API_URL=http://localhost:8080/api/v1

# Google OAuth client ID (obtain from Google Cloud Console → OAuth 2.0 client IDs)
# Create separate client IDs for iOS and Android if using native SDK (Phase 4)
# For expo-auth-session (this milestone), a single web client ID works
EXPO_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
